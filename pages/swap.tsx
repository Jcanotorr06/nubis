import type { NextPage } from 'next'
import { SwapWidget } from '../components'

const Swap:NextPage = () => {
    return (
        <>
            <SwapWidget/>
        </>
    )
}

export default Swap
